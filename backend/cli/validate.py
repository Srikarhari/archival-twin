"""Database validation CLI: check integrity, find orphans, verify embeddings."""
# TODO: CLI entry point — python -m cli.validate --db PATH
# TODO: Check every filename in DB exists on disk
# TODO: Verify embedding byte length (2048 = 512 * float32)
# TODO: Report orphaned DB rows and un-ingested files
# TODO: --prune flag to remove orphaned rows
